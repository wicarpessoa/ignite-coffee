import styled from 'styled-components'

export const FormErrorDescriptionContainer = styled.span`
  display: block;
  font: (--roboto-xs);
  color: #ff1832;
  font-size: 0.75rem;
  margin-top: 0.25rem;
`
